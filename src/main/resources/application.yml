spring:
  application:
    name: astr-ai-code-mother-backend
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/astr-ai-code
    username: root
    password: Paramecium88.
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi项目配置
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.astr.astraicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn