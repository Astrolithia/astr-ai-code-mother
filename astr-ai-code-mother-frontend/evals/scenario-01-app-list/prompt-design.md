# 场景一 · 实验组提示词（加载 design.md）

运行前确认 `design.md` 在项目根目录，且 `AGENTS.md` 里的引用段落还在。
以下内容原样发给 agent，不要临场增删。

---

搭好全局布局，并实现「我的应用」页面。

- 全局布局：顶部导航（站点名 Astr、主导航、当前用户）、中间路由内容区、底部版权行。
- 「我的应用」页面：展示用户已创建的应用，让他能看清每个应用现在处于什么状态，并对单个应用执行操作。
- 数据在 `evals/scenario-01-app-list/data.json`，直接读这个文件，不要编造任何数据。
